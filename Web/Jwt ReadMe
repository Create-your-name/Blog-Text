jwt  可以生成一个 加密的 token 作为用户登入的令牌 登入成功 发送给客户端

请求需要的资源或接口时 将token携带，后端验证token是否合法

jwt有三部分 ：A B C
    A ： Header {"type": "jwt" : "HS256" } 固定
    B :  playload ,存放信息 比如 用户id  过期时间等等， 可以被解密 不能存放敏感信息
    C :  签证 , A 和B 加上秘银 加密而成  只要秘银不丢失 就可以认为是安全的
    jwt 验证  主要验证C 部分 是否合法